# Demo mode: runs the real sync engine against an in-memory server so the
# windows can be previewed with no Supabase project. No secrets; committed on
# purpose. Started with `pnpm dev:demo` inside apps/desktop.
VITE_RECALL_DEMO=1
