{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Recall's windows. Window behaviour, the tray, the hotkey and autostart all live in Rust and are reached through application commands, so the web layer needs very little. The one exception is dragging: a frameless window is moved by the webview calling startDragging, which needs its own permission.",
  "windows": ["main", "widget", "quick"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "opener:default"
  ]
}
