# Demo mode: runs the real sync engine against an in-memory server so the whole
# interface works with no Supabase project. Contains no secrets and is committed
# on purpose. Started with `pnpm dev:demo`.
VITE_RECALL_DEMO=1
